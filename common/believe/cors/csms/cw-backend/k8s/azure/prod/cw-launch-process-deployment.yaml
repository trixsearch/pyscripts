apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cw-launch-process
  name: cw-launch-process
  namespace: cw
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cw-launch-process
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cw-launch-process
    spec:
      imagePullSecrets:
        - name: cwms-jfrog 
      containers:
        - command: ["python3"]
          args: ["consumers/launch_event_controller.py"]
          image: devopsartifact.jio.com/jpf-cwms__prod__dcr/cw-backend/consumers:#{Build.BuildId}#
          imagePullPolicy: Always          
          name: cw-launch-process
          env:
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: cw-prod-config
                  key: KAFKA_BROKERS
            - name: BACKEND_DOMAIN_URL
              valueFrom:
                configMapKeyRef:
                  name: cw-prod-config
                  key: BACKEND_DOMAIN_URL
            - name: ENV
              valueFrom:
                configMapKeyRef:
                  name: cw-prod-config
                  key: ENV
            - name: SSL_VERIFICATION
              valueFrom:
                configMapKeyRef:
                  name: cw-prod-config
                  key: SSL_VERIFICATION
            - name: LAUNCH_PROCESS_TOPICS
              valueFrom:
                configMapKeyRef:
                  name: cw-prod-config
                  key: LAUNCH_PROCESS_TOPICS
            - name: CELERY_BROKER_URL
              valueFrom:
                secretKeyRef:
                  name: cw-prod-secret
                  key: CELERY_BROKER_URL
          resources:
            requests:
              cpu: 2000m
              memory: "3Gi"
            limits:
              cpu: 3000m
              memory: "4Gi"
      volumes:
        - name: tmp-files
          emptyDir: {}
      restartPolicy: Always
