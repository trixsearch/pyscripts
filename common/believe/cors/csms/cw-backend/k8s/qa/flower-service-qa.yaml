apiVersion: v1
kind: Service
metadata:
  labels:
    app: cw-flower
  name: cw-flower
  namespace: qa
spec:
  type: ClusterIP
  ports:
    - name: "cw-flower"
      port: 80
      targetPort: 5555
  selector:
    app: cw-flower-qa
